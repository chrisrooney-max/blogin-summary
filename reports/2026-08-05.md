## Blogin Summary — no new posts (0 pass the tag allowlist) *(first LIVE run — blogin MCP now authenticated)*

This is the first run against real blogin data instead of `fixtures/posts.json`. `state/seen-posts.json` was reseeded with the real blogin ids for the three posts already summarized in stub mode (SDD post → `320373`, coding-agent-swarm → `322896`, AI Code Fest → `322780`), so they won't be re-reported.

3 posts skipped — none matched `#gen-ai` / `#tech-community` / Uncategorized:
- "The Conversation After the Conversation" — `#community`
- "Advice Process July 2026: Monthly report" — `#advice-process`, `#news`
- "5 Minutes With…Poorva Gokhale, Developer, EE India" — `#community`

None were added to `seen_ids`, so they'll be re-evaluated if their category ever changes.
