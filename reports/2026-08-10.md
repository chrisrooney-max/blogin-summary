## Blogin Summary — no new posts since 2026-08-05T18:27:02Z *(stub run against fixtures/posts.json — blogin MCP server not found/authenticated)*

`fixtures/posts.json` is unchanged from previous runs. All 5 fixture posts map to posts already covered by earlier reports, so nothing new passes the tag allowlist:

- **AI + Specification-Driven Development (SDD)...** (`sdd-ai-blog`) — already summarized and flagged as a POV candidate on 2026-07-30; corresponds to real blogin id `320373`, already in `seen_ids`.
- **Running a Coding-Agent Swarm From a Chat Window** (`coding-agent-swarm`) — already summarized on 2026-07-30 (not flagged — no client-facing sales hook); corresponds to real id `322896`.
- **AI Code Fest** (`ai-code-fest-hmrc`) — already summarized and flagged as a POV candidate on 2026-08-03; corresponds to real id `322780`.
- **Notes From Friday's Cross-Team Retro** (`friday-retro-notes`) — already summarized on 2026-07-30 (Uncategorized, not flagged); this one is a fixture-only test post with no real-blogin equivalent, so its fixture id has been added directly to `seen_ids` this run to prevent it being re-reported.
- **Our London Office Is Moving Floors** (`office-move-announcement`) — still tagged `facilities`, outside the #gen-ai / #tech-community / Uncategorized allowlist; skipped again, not added to `seen_ids`.

`seen_ids` now carries both the real-blogin ids (from the 2026-08-05 live run) and the fixture ids for the four already-covered posts, so future runs recognize them regardless of whether live or stub data is active.
