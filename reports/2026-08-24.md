## Blogin Summary — no new posts since 2026-08-17T09:00:00Z *(stub run against fixtures/posts.json — blogin MCP server not found/authenticated)*

`fixtures/posts.json` is unchanged from the previous run. All fixture posts that pass the tag allowlist are already in `seen_ids`:

- **AI + Specification-Driven Development (SDD)...** (`sdd-ai-blog`) — already summarized and flagged as a POV candidate on 2026-07-30; corresponds to real blogin id `320373`.
- **Running a Coding-Agent Swarm From a Chat Window** (`coding-agent-swarm`) — already summarized on 2026-07-30 (not flagged — no client-facing sales hook); corresponds to real id `322896`.
- **AI Code Fest** (`ai-code-fest-hmrc`) — already summarized and flagged as a POV candidate on 2026-08-03; corresponds to real id `322780`.
- **Notes From Friday's Cross-Team Retro** (`friday-retro-notes`) — already summarized on 2026-07-30 (Uncategorized, not flagged); fixture-only test post with no real-blogin equivalent.
- **Our London Office Is Moving Floors** (`office-move-announcement`) — still tagged `facilities`, outside the #gen-ai / #tech-community / Uncategorized allowlist; skipped again, not added to `seen_ids`.

No blogin MCP tools were found this run (checked via `ToolSearch`), so this ran in stub mode. `seen_ids` is unchanged from the last run; only `last_checked` was updated.
